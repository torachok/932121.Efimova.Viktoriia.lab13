@model List<QuizQuestion>

@{
    ViewData["Title"] = "Hello Quiz";
}

<h1>Quiz results</h1>

<p>Right answers @ViewBag.RightAnswers out @Model.Count </p>

<ol>
    @for (int i = 0; i < Model.Count; i++)
    {
        <li>@Model[i].Question @Model[i].UserAnswer</li>
    }
</ol>